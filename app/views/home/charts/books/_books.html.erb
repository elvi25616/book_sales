<section class="flex w-full flex-col border">
    <h3 class="text-white font-bold text-lg">Sells by book</h3>
    <div class="flex flex-col" id="books_charts">
    <% Book.all.each do |book| %>
      <%= render 'home/charts/books/chart', book: book %>
    <% end %>
    </div>
  </section>